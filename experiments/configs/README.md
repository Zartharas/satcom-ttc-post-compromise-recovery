# Experiment Configurations

Machine-readable experiment configurations live here.

Current paper contract:

- `paper-final-experiment.json` — predeclared final-study plan used by retained run
  `20260814T022506Z-gc630fb4`.

Files named `phase-*` are historical development/configuration records. Some intentionally
remain marked provisional, candidate, or pilot-only because those were their states when used.
Do not globally rewrite those statuses after outcome observation.
