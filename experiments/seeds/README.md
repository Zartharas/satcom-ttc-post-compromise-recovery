# Seeds and Schedule Notes

This directory contains historical seed/schedule notes from experiment development.

The final Study C seed population (`10001` through `10100`) and the authoritative serialized
Study B/C/D schedules are embedded in
`experiments/configs/paper-final-experiment.json`. For reproducibility, the serialized schedule
and its SHA-256—not the integer seed alone—define the final replay artifact.
