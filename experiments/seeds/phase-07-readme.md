# Phase 07 Seed Records

Phase 07 uses integer seeds only as generator inputs. The authoritative replay record is the complete serialized fault schedule and its SHA-256 digest stored in each JSON result.

No seed set is frozen in this phase. The provisional seed list is defined in `experiments/configs/phase-07-provisional.json` and may change during internal development.
